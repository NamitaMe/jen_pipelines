stage('Build') {
            steps {
                sh 'echo "I am on main branch"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
